import { Fragment } from 'react';
import { Header } from '@components/Header';

import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  )
}

export default Home
